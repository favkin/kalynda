Drop your logo file here as one of:
  - logo.png
  - logo.svg  (rename it to logo.png regardless of format is NOT required if you edit Logo.jsx,
                but the simplest path is: export/save your logo as logo.png and place it in this
                /public folder)

Recommended: transparent background, roughly 400x120px (or similar wide aspect ratio) so it
sits well in the navbar. The site will pick it up automatically once the file exists at
/public/logo.png — no code changes needed.
